need to build a system that makes an AI system outputs only necessary parts 
    ⇒ 지금 LLM은 답변에서 유저가 원하는 부분만 골라서 메모리에 저장해놓고 나머지는 기억하지 않도록 하는게 안되기 때문에 이 부분을 고쳐서 내가 원하는 부분만 elaborate/나중을 위해 사용하고 나머지는 패스하는게 나을것 같다 (기능)
    ⇒ user 가 course notes 자체에 error가 있는지 같은 critical thinking 이 필요
    
    elaboration tolerance problem → instead of making the LLM output too much output context, make it output short lists of concepts so that it becomes less complex to use ***over time*** rather than making it harder to use over time 
    
    mind map 말고 **boring, effective, *simple*** 한거에 집중해라
    
    initial state - credit assignment problem 문제를 잘 정의하고 그거에 맞는 솔루션을 만든다고 생각하자